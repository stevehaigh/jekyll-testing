# Site Build Settings

# Jekyll-paginate-v2 configuration
#  pagination:
#   enabled: true
#   per_page: 5
#   permalink: '/page/:num/'
#   title: ':title - page :num of :max'
#   limit: 0
#   sort_field: 'date'
#   sort_reverse: true

# jekyll-paginate configuration
##paginate:            5

# Build settings
markdown:            kramdown
highlighter:         rouge
permalink:           none
plugins:             [jekyll-paginate-v2, jekyll-sitemap, jekyll-feed, jekyll-seo-tag]

# Collections
collections:
  routes:
    output: true

# Customise atom feed settings (this is where Jekyll-Feed gets configuration information)
title:		           '50 Classic Ski Descents of the Alps'
description:	       'A subjective list of classic ski runs in the European Alps.'
author:              'Steve Haigh'

# RSS 2.0 can be used instead of Atom by uncommenting following two lines
#feed:
#path: rss-feed.xml
